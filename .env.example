# ─── Database ─────────────────────────────────────────────────────────────────
# Development (matches docker-compose.yaml):
DATABASE_URL="postgresql://user:password@localhost:5432/app_mierda?schema=public"
#
# Production recommendations:
#   • Enable TLS:          append ?sslmode=require
#   • Limit connections:   append &connection_limit=10  (tune to your plan)
#   • Use a pooler:        point to PgBouncer / Supabase pooler / Neon pooler
#   Example:
#   DATABASE_URL="postgresql://user:pass@db.example.com:5432/mydb?schema=public&sslmode=require&connection_limit=10"

# ─── NextAuth ─────────────────────────────────────────────────────────────────
# Generate a secret: openssl rand -base64 32
AUTH_SECRET="your-secret-here"
# Development: http://localhost:3000
# Production:  your public HTTPS URL — must match the OAuth redirect URI
AUTH_URL="http://localhost:3000"

# Google OAuth — https://console.cloud.google.com/
# Authorized JavaScript origins:    https://yourapp.com
# Authorized redirect URIs:         https://yourapp.com/api/auth/callback/google
AUTH_GOOGLE_ID="your-google-client-id"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# ─── Cloudinary ───────────────────────────────────────────────────────────────
# https://console.cloudinary.com/
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"


NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""